import { FiltroPdvCodigoPipe } from './filtro-pdv-codigo.pipe';

describe('FiltroPdvCodigoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPdvCodigoPipe();
    expect(pipe).toBeTruthy();
  });
});
